<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.diplom1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Диплом"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TrenActivity" />
        <activity android:name=".UprActivity" />
        <activity android:name=".PodrActivity" />
        <activity android:name=".ProdActivity" />
        <activity android:name=".ProdPodrActivity" />
        <activity android:name=".CalActivity" />
        <activity android:name=".UpdTrenActivity"></activity>
    </application>

</manifest>